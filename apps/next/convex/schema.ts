import { defineSchema } from "convex/server";

// The app schema is intentionally minimal — auth tables live inside
// the convexAuth component and are not visible here.
export default defineSchema({});
